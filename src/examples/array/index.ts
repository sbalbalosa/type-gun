/**
 * 
 * @array
 * class Person {
 *     @field
 *    name: string;
 * }
 * 
 * const person = Person.create(rootNode, index); // index is optional
 * person.name = "John Doe";
 * await person.save();
 * 
 * 
 * 
 */