// import './keychain';
import './read-access';
// API SAMPLE

// import { encrypted, field, keychain } from "../../decorators";
// import Keychain from "../../decorators/lib/keychain";

// @keychain
// class Person {    
//     @field
//     name: string;

//     @field
//     @encrypted
//     email: string

//     @field
//     @encrypted
//     address: string;
// }

// const keychain = Keychain.create(userNode, Person);
// keychain.pub(pair.pub);
// keychain.epub(pair.epub);
// await keychain.save();

// const person = Person.create(rootNode);
// person.name = "John Doe";
// person.email = "test@test.com";
// person.address = "123 Main St";
// await person.attachKeychain(keychain);
// await person.save();

